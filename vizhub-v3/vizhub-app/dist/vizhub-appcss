.viz-page {
  flex: 1;
  display: flex;
  flex-direction: column;
  height: 100%;
  /* https://stackoverflow.com/questions/38382734/flex-items-not-shrinking-when-window-gets-smaller */
  min-height: 0;
}
.viz-page-split {
  flex: 1;
  display: flex;
  height: 100%;
  min-height: 0;
}

.topbar {
  /* Oddity, height was not working without this. */
  flex-shrink: 0;
  height: 30px;
  width: 100%;
  background-color: white;
  display: flex;
  justify-content: space-between;
  padding: 0 0.75rem 0 0;
}
.topbar-editor-toggle {
  height: 30px;
  font-weight: bold;
  padding: 0 0.75rem 0 0;
  display: flex;
  align-items: center;
}
.topbar-part {
  display: flex;
  align-items: center;
}

.editor-sidebar {
  width: 100%;
  font-family: var(--vizhub-font-family-monospace);
  display: flex;
  flex-direction: column;
  background-color: var(--vizhub-editor-bg-dark);
}
@media (min-width: 576px) {
  .editor-sidebar {
    width: 200px;
  }
}
.editor-sidebar-items {
  background-color: var(--vizhub-editor-bg-light);
}
.editor-sidebar-item {
  color: var(--vizhub-editor-color);
  padding: 0 0.75rem;
  display: flex;
  align-items: center;
  height: var(--vizhub-editor-entry-height);
  border-left: 5px solid transparent;
}
.editor-sidebar-item:hover, .editor-sidebar-item:focus {
  color: var(--vizhub-editor-color);
}
.editor-sidebar-item.active {
  border-left: 5px solid white;
}

.editor-content {
  flex: 1;
  flex-direction: column;
  background-color: var(--vizhub-editor-bg-darkest);
  font-family: var(--vizhub-font-family-monospace);
  color: var(--vizhub-editor-color);
}
.editor-content-header {
  height: var(--vizhub-editor-entry-height);
  background-color: var(--vizhub-editor-bg-dark);
  display: flex;
  justify-content: space-between;
}
.editor-content-header-left {
  padding-left: 0.75rem;
}
.editor-content-header-left, .editor-content-header-right {
  display: flex;
  align-items: center;
}

.vertical-split-right {
  display: flex;
  flex: 1;
  flex-direction: column;
  align-items: center;
  min-width: 0;
  height: 100%;
  overflow: auto;
}
.vertical-split-right .viz-viewer {
  /* Scale horizontally when width < max-width. */
  width: 100%;
  /* Ensure that the _inner_ content has max-width: vizhub-content-max-width, */
  /* while the _outer_ content shrinks to preserve 10px horizontal padding. */
  max-width: calc(var(--vizhub-content-max-width) + 2 * var(--vizhub-padding-little));
  padding: var(--vizhub-padding-little);
}
.vertical-split-right .viz-viewer svg.viz-frame {
  background-color: white;
  border-radius: var(--vizhub-border-radius);
  box-shadow: var(--vizhub-box-shadow);
  /* This in conjunction with viewBox causes */
  /* the rectangle to fill the appropriate width */
  /* _and_ preserve aspect ratio.*/
  width: 100%;
}
.vertical-split-right .viz-viewer .title {
  padding: var(--vizhub-padding-lots) 0px;
  border-bottom: 1px solid var(--vizhub-rule-color);
}

/*# sourceMappingURL=vizhub-appcss.map */
