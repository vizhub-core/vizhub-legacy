{
  "scripts": {
    "test": "cd packages/useCases && npm test && cd ../gateway && npm test && cd ../presenter && npm test && cd ../controller && npm test && cd ../database && npm test && echo 'All tests pass!'"
  },
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
