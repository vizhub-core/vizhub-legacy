import assert from 'assert';
import {
  CreateVisualization
} from '../dist';


describe('Use Cases', () => {
  describe('Create Visualization', () => {
    it('should expose expected fields', () => {
      // TODO
    });
  });
});
